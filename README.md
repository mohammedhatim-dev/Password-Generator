# Password Generator
A simple Python script that generates safe, random passwords based on a user-defined length.

## Features
* Combines letters, numbers, and symbols.
* Uses Python's random module for safe randomization.
* Flexible password length specification.
